import { delay } from '../utils/delay';

export async function addCreatedArticleToList(): Promise<void> {
    // TODO: add the article to the wikiproject's created-articles list
    await delay(1000);
}
